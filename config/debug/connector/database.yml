connector:
  checkpointer:
    name: conversation_checkpointer
    type: database
    engine: sqlite
    host: ${oc.env:CHECKPOINT_DB_HOST}
    port: 0
    default_name: ${oc.env:CHECKPOINT_DB_NAME}
    pool: {}
    auth:
      type: none
